OCR + Parsing: Convert PDFs → structured JSON; detect question segmentation, marks, year.

Question Normalization:
De-duplicate variants (parameterized numeric parts).
Extract entities, formulas, required sub-parts.

Syllabus Alignment:
Map each normalized question to syllabus node (topic/chapter).
Detect missing-yet-likely topics (negative space analysis).

